# Решение проблем синхронизации данных

## Требования для синхронизации:

1. **Приложение на телефоне должно быть установлено и запущено**
   - Установите `app-debug.apk` на телефон
   - Запустите приложение LazyBones на телефоне
   - Приложение должно оставаться открытым или работать в фоне

2. **Часы должны быть подключены к телефону**
   - Подключите часы к телефону через Bluetooth или Wi-Fi
   - Убедитесь, что часы синхронизированы с телефоном через Wear OS приложение

3. **Проверка подключения**
   - На часах в приложении будет показана информация о подключении
   - "📱 Подключено" - значит устройства видят друг друга
   - "⚠️ Устройства не подключены" - нужно проверить соединение

## Диагностика:

### Логи с телефона (отправка данных):
```bash
adb logcat | grep WearDataSync
```

Должны увидеть:
- `📱 Checking connection`
- `📤 Starting sync`
- `✅ Data synced successfully`

### Логи с часов (прием данных):
```bash
adb -s <device_id> logcat | grep -E "WearDataListener|MainActivity"
```

Должны увидеть:
- `📥 onDataChanged`
- `✅ Parsed data`

## Если синхронизация не работает:

1. Проверьте, что оба приложения установлены
2. Убедитесь, что приложение на телефоне запущено
3. Проверьте подключение Bluetooth/Wi-Fi между устройствами
4. Перезапустите оба приложения
5. Проверьте логи для диагностики ошибок

