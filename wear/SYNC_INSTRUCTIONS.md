# Инструкция по синхронизации модуля Wear в Android Studio

Если Android Studio не видит модуль `wear`, выполните следующие шаги:

## Способ 1: Gradle Sync

1. Откройте проект в Android Studio
2. Нажмите **File → Sync Project with Gradle Files** (или кнопка 🐘 в панели инструментов)
3. Дождитесь завершения синхронизации
4. Модуль `wear` должен появиться в списке модулей

## Способ 2: Пересоздание конфигурации

1. Откройте **Run → Edit Configurations...**
2. Нажмите **+ → Android App**
3. Укажите:
   - **Name:** Wear OS Debug
   - **Module:** wear (выберите из списка)
   - **Deployment Target:** выберите Wear OS устройство/эмулятор
4. Нажмите **OK**

## Способ 3: Invalidate Caches

Если модуль все еще не виден:

1. **File → Invalidate Caches / Restart...**
2. Выберите **Invalidate and Restart**
3. После перезапуска выполните **Gradle Sync**

## Проверка модуля

После синхронизации убедитесь, что:
- Модуль `wear` виден в **Project** view (Android)
- Можно выбрать `wear` в конфигурации запуска
- Gradle задачи доступны: `./gradlew :wear:tasks`

## Альтернатива: Запуск через Gradle

Если конфигурация не работает, используйте:

```bash
# Сборка
./gradlew :wear:assembleDebug

# Установка (требует подключенное устройство)
./gradlew :wear:installDebug

# Или через скрипт
./wear/build-watch.sh
```

