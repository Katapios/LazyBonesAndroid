## LazyBones Bugfix Checklist

- [x] MainViewModel: убрать бесконечный `while(true)` без отмены, заменить на управляемый таймер/WorkManager, чтобы не держать корутину вечно.
- [x] WearDataSyncService: добавить отмену/дедупликацию задач синхронизации и таймауты для `Tasks.await`, чтобы не копить зависшие синки.
- [x] Оптимизировать передачу данных в Data Layer (отправлять только изменения, хранить последнюю версию/хэш) во избежание постоянной пересылки всех планов и отчётов.
- [x] Централизовать обработку данных на Wear (оставить один слушатель и единый репозиторий), убрать дублирование между Activity/Service/ListenerService.
- [x] Синхронизировать запись в SharedPreferences на часах или перейти на DataStore, чтобы избежать гонок и потери данных.
- [x] Исправить NotificationService: гарантированно отменять все старые PendingIntent при смене режима уведомлений.
- [x] Добавить тесты для TimePoolManager/статуса отчётов/JSON синка и предусмотреть защиту TelegramService от rate limit (ретраи/лимитер).

